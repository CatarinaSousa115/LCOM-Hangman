#ifndef __MENU_H__
#define __MENU_H__


/*void draw_text(WINDOW *win, int y, int x, const char *text) {
  if (win == NULL) {
    win = stdscr; // Use the default screen if no window is provided
  }
  mvwprintw(win, y, x, "%s", text); // Print text at (y, x) in the window
  wrefresh(win);                    // Refresh the window to display the changes
}
*/
#endif